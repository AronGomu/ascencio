# T5 retry1 repair — checked, independent review required

Next action: review `artifacts/T5-REPAIR-EVIDENCE/repair-source-tests.diff` against B1/B2/B3/N1 in `artifacts/REVIEW-T5.md`.

## State / evidence

- S1. **Implemented** accepted B1/B2/B3/N1 only. Parent-registered route `openai-codex/gpt-6-astra:high`, retry1 per N3. Sole root writer; no subagents, staging, commits, pushes, deployment, T6 implementation, public signature changes, or alias changes.
- S2. **Focused green: 91/91** = original 55 plus 36 added cases. `artifacts/T5-REPAIR-EVIDENCE/green-vitest.log`.
- S3. **Affected regressions: 66/66.** `artifacts/T5-REPAIR-EVIDENCE/regressions.log`; legacy passing test emits exact `CONTENT_RESPONSE_CANCEL_FAILED`.
- S4. **Typecheck: 0 errors / 4 pre-existing warnings.** Full T5 targeted ESLint/Prettier passed; diff check passed; staging empty. Exact cmds, exit codes, output references: `artifacts/T5-REPAIR-EVIDENCE/commands.json`, `exit-codes.txt`, `no-staged-files.txt`.
- S5. **Native Chromium 149.0.7827.55 passed.** Two-page local source fixture uses real Web Locks, Cache Storage, IDB, HTTP requests. Evidence: `artifacts/T5-REPAIR-EVIDENCE/browser-native-result.json`, `browser-native-trace.zip`; rerun `node artifacts/T5-REPAIR-EVIDENCE/browser-native.mjs`.

## Findings repaired

| Finding                 | Implementation evidence                                                                                                                                                                                                                                                                                                                          | Test evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1 nested lock deadlock | `src/content/storage/progressive-content-store.ts:229`: removed global `ygo-content-download-v1` acquisition; retained nonqueued exclusive `ygo-content-download-job-v1:<jobId>`.                                                                                                                                                                | `tests/unit/progressive-download.test.ts:171,199`: Shell-held exclusive harness completes; cross-store same-job rejects `CONTENT_JOB_CONFLICT`; live job remains running on reopen. Native Chromium verifies same behavior across two pages; first download completes within bounded wait.                                                                                                                                                                                                                    |
| B2 cache orphans        | `src/content/storage/progressive-cache-ownership.ts:5`; `src/content/storage/progressive-content-store.ts:510,545`: enumerate cache keys, require canonical GET/current-app base/hash/path identity, compare full manifest allow-set. Never trust a file row's arbitrary cache URL for deletion. Observe cleanup transaction completion/failure. | `tests/unit/progressive-storage.test.ts:71,104,148,185`: immediate quota-orphan deletion; unused media/unknown/noncanonical/legacy preservation; both cleanup methods retry partial cache deletion; IDB clear fault rejects without unhandled rejection, retry succeeds. `tests/unit/progressive-download.test.ts:495`: unused chapter-02 required files survive despite no file rows. Native injected IDB quota follows successful real Cache.put: orphan exists, row absent, immediate deletion removes it. |
| B3 late abort           | `src/content/storage/progressive-content-store.ts:364,367`: check abort after workers/persistence, before complete transition; recheck after complete write so abort during that await persists paused before rejection.                                                                                                                         | `tests/unit/progressive-download.test.ts:249`: controlled final Cache/file-row/progress/complete write delays; abort yields `CONTENT_CANCELLED`, persisted/reopened paused, no complete progress event. Native final Cache.put abort also persists paused.                                                                                                                                                                                                                                                    |
| N1 corrupt jobs         | `src/content/storage/persisted-download-job.ts:32`; `src/content/storage/progressive-content-store.ts:269,485,576`: central exact-shape parser validates IDB key/UUID/request/manifest closure/order/kind/progress identity/phase/safe counters/bounds/manifest-derived totals. List/open/resume all validate; returned jobs/list frozen.        | `tests/unit/progressive-storage.test.ts:204,312`: 22 malformed-row cases reject `CONTENT_INTEGRITY_FAILED` on list/open/resume, preserve exact metadata, issue no network requests. `tests/unit/progressive-download.test.ts:327`: missing dependency/unsorted closure rejected. Existing interrupted-running fixture now references real cached manifest with valid totals.                                                                                                                                  |

## TDD / bounded repair

- T1. Tests written before production repair. Exact focused cmd first returned exit 1: **35 failed, 55 passed**, plus unhandled `AbortError` from injected IDB clear abort. `red-vitest.log` retains full failures. This includes revised global-lock expectation; cross-store red also exposes old test fixture's unsupported shared-lock nesting, not a claim that native shared/shared exclusion failed.
- T2. Minimal production changes yielded **90/90**, no unhandled errors. `green-vitest-attempt1.log`. Cleanup now observes `tx.done` alongside request work; fault no longer escapes as unhandled rejection.
- T3. Initial typecheck returned exit 2: `error TS2352: Conversion of type 'RegExpExecArray' to type '[string, string, string]' may be a mistake because neither type sufficiently overlaps with the other. If this was intentional, convert the expression to 'unknown' first.` Replaced tuple cast with proven regex group indexing; final typecheck passes.
- T4. Added unused required-chapter allow-set assertion after green to strengthen existing cleanup proof; **91/91** final. This extra case was not claimed as an independent red implementation failure. No production refactor after green beyond regex type correction/formatting.
- T5. Kept `TestLockManager` unchanged: repaired Content path needs its exclusive/nonqueued semantics only. No claim that fixture models browser queue/shared-lock semantics. Native Chromium covers actual integration semantics separately.

## Changed files — retry1 only

- F1. `src/content/storage/progressive-content-store.ts` — lock ownership, persisted-job validation integration, late cancellation, cache enumeration, observed cleanup transactions.
- F2. `src/content/storage/persisted-download-job.ts`; `src/content/storage/progressive-cache-ownership.ts` — two focused private storage concerns; no public exports added.
- F3. `tests/unit/progressive-download.test.ts`; `tests/unit/progressive-storage.test.ts` — 36 added cases plus corrected existing canonical-key/running-job/lock expectations.
- F4. `artifacts/IMPLEMENTATION-LEDGER-T4-through-T11.md`; `artifacts/PLAN_2026_09_13_content_module_rearchitecture/T5_progressive-storage.md` — retry1 route/findings/checklist evidence, pending independent acceptance.
- F5. `artifacts/IMPLEMENTATION-REPORT-T5-repair.md`; `artifacts/T5-REPAIR-EVIDENCE/` — report, before snapshots, repair-only diffs, runnable native fixture, logs/results/trace, command/exit manifest, status/staging evidence. Ignored `graphify-out/` refreshed via required graph update; no generated graph staged.

Pre-existing T5 contracts/public aliases/legacy consumer edits preserved. `src/content/storage/progressive-storage-validation.ts` and `tests/fixtures/progressive-storage.ts` unchanged by retry1; before copies retained for inspection. `artifacts/REVIEW-T5.md` remains byte-unedited by this worker.

## Assumptions

- A1. T9 binding Shell runner owns global exclusive download lock; Content must not reacquire it. Cleanup remains caller-held application-exclusive → download-exclusive contract. No Shell runner/API added here.
- A2. Dedicated progressive cache plus exact current-app canonical GET key establishes ownership, including bodies without DB receipts. Unknown/noncanonical/foreign-base keys and legacy caches/DBs are preserved, even when a malformed file row points at an unknown URL.
- A3. Persisted malformed identity/closure/metadata is integrity failure, not a resume mismatch or recoverable empty list. Missing/corrupt referenced manifest fails closed. No corrupt-row deletion, normalization repair, fabricated progress, receipt, or success.
- A4. Existing dirty/untracked root work belongs prior accepted lanes/T5 attempt. Narrow repair touches only listed paths; no baseline reconciliation or unrelated cleanup.

## Failure handling / residual risks

- R1. Native harness is **not** production UI/T9/T10 integration, full app E2E, real quota exhaustion, disk eviction, or browser process-crash coverage. Quota is injected at IDB file-row write after actual Cache.put. Chromium host-validation warning inherited env `PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS`; launched browser completed assertions.
- R2. Cleanup remains intentionally non-atomic across Cache/IDB: failure can leave partial deletion, surfaces fixed Content error, supports explicit retry. Caller must still hold both external locks; T9/T10 not implemented here. Existing no-Web-Locks fallback unchanged; no new fallback or fake cross-tab guarantee.
- R3. Full repo suite/build/product E2E not run. Requested T5, affected 66, typecheck, full T5 targeted quality gates passed. Separate Markdown Prettier probe found ledger/ticket formatting warnings, reproduced against both before snapshots (`doc-prettier-baseline.log`); preserved per narrow-edit scope. New report formatted. Independent reviewer acceptance remains mandatory; implementation report does not grant acceptance.
- R4. New catch in ownership parser returns null only for structurally non-owned keys; it does not swallow storage I/O. Persisted parser maps invalid/missing manifest to fixed integrity failure, rethrows storage failures. Added paths contain no `|| true`, empty catch, redirected-and-ignored validation failure, or unobserved Promise. Native harness observes download rejections explicitly; shell captures every validation exit.
- R5. Removed only own `.tmp/t5-repair-vite/` scratch after reading generated files; exact file list in `artifacts/T5-REPAIR-EVIDENCE/scratch-removed.json`. Before snapshots/logs/trace retained as deliverables. Graph cache retained as requested repository knowledge output.

Next action: independent reviewer opens `artifacts/T5-REPAIR-EVIDENCE/repair-source-tests.diff`.
